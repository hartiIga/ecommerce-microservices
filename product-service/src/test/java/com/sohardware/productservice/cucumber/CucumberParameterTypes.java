package com.sohardware.productservice.cucumber;

import io.cucumber.java.ParameterType;

import java.math.BigDecimal;


public class CucumberParameterTypes {

    // no need here ca existe dejà, mais je garde pour montrer qu'on peut créer nos var cucumber personnalisés
    // Cette méthode définit le nouveau mot-clé {bigdecimal} utilisable dans vos Steps
//    @ParameterType("[0-9]+\\.[0-9]+") // Expression régulière qui détecte un nombre décimal
//    public BigDecimal bigdecimal(String value) {
//        return new BigDecimal(value);
//    }
}
